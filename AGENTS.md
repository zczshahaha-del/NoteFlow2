# NoteFlow 项目协作规则

本文件适用于整个 NoteFlow 仓库。任何在本仓库工作的 Codex 任务都必须遵守以下规则。

## 开发问题记录

1. 开始开发前先阅读 `docs/NoteFlow-开发问题汇总.md`，避免重复踩坑或把未完成事项误判为已解决。
2. 开发、测试、设计或部署过程中一旦确认新问题，必须在同一轮工作中追加到上述汇总文档。
3. 已有问题的状态、根因或解决方案发生变化时，更新原条目，不重复创建同义问题。
4. 每条问题记录至少包含：问题现象、根因、处理方式、当前状态和验证证据；能关联代码时补充相关文件或提交说明。
5. 未真正完成运行验证的问题只能标记为“部分解决”或“未解决”，不能因为已有设计稿、代码或脚本就标记为“已解决”。
6. 不在问题文档中记录密码、授权码、API Key、私钥或其他敏感信息。

## Git 提交与备注

1. 每一轮产生的有效文件改动都必须提交 Git；只读检查且没有文件变化时不创建空提交。
2. 一次提交只包含一个清晰主题，不混入无关改动，也不覆盖用户已有的未提交内容。
3. 提交标题应准确说明类型和范围，例如 `feat:`、`fix:`、`design:`、`docs:`、`test:`、`refactor:`、`ops:`。
4. 非简单修改必须使用提交正文备注，至少写清：
   - 改了什么；
   - 为什么修改；
   - 做了哪些验证；
   - 仍有哪些限制或风险。
5. 提交前必须检查 `git diff`、`git diff --check` 和 `git status`，并执行与风险相匹配的测试或构建。
6. `.env`、数据库、附件、缓存、日志和本地密钥不得提交；只提交脱敏的模板、脚本和说明。
7. 完成并验证后，将提交推送到当前跟踪远端；如果推送失败，要明确报告本地提交号和失败原因。
8. 交付时必须说明提交号、推送状态、验证结果和未解决事项。

## 问题记录与提交的配合

1. 修复历史问题时，在同一提交中更新问题条目的状态与验证信息。
2. 新问题暂时无法修复时，也要将其记录为未解决，并说明阻塞条件和下一步。
3. 若一个任务包含多个独立问题，按问题或模块拆成多条提交；问题文档可以在最后一条文档提交中统一补齐关联信息。
4. 任何“已完成”结论都以仓库状态、测试结果和目标环境验证为准，不以对话中的口头确认代替。

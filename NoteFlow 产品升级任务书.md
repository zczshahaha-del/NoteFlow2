# NoteFlow 产品升级任务书

版本：v1.0  
建立日期：2026-07-15  
执行方式：按优先级逐项实施；每项完成后更新状态、验证结果和实施记录。

## 当前进度结论

截至 2026-07-15，本任务书 P0–P4 约定范围已全部完成并通过交付验收。最终前后端运行在 `5173/8080`，数据库迁移为 `20260715_0004 (head)`；Cookie 会话、真实 AI 流式回答、后台索引、搜索、草稿、修改预览、记忆、双设备冲突、离线壳和 PWA 安装资源均已验证。新版 Tiptap 已作为默认编辑器，历史语料 27 条中 25 条直接通过，2 条超长内容由保真门禁自动降级到经典编辑器，未发现无保护的数据风险。网页剪藏/OCR、桌面封装和本地文件双向同步已完成评估并明确后置，不属于当前版本缺陷。

## 1. 项目目标

将 NoteFlow 从“功能完整的 AI 笔记项目”升级为“面向开发者、学习者和求职者的 AI 原生个人知识工作台”。

目标不是复制 Notion，而是在保证基础笔记体验可靠、美观、易用的前提下，突出以下差异化能力：

1. 基于个人知识库的准确检索与问答。
2. 带来源引用的 RAG 回答。
3. 分阶段、可确认的 AI 笔记草稿生成。
4. 先预览、后应用的 AI 安全修改。
5. 可控、可编辑、可关闭的长期记忆。
6. 可追踪、可恢复的 Agent 任务执行。

## 2. 执行原则

1. 先修阻塞使用的问题，再增加新功能。
2. 先保证中间笔记区始终可用，再考虑左右侧栏常驻。
3. 每个功能必须同时考虑空状态、加载、失败、重试和窄屏。
4. 不绕过用户确认直接修改正式笔记。
5. 不破坏已有结构化笔记、版本、RAG、草稿、记忆和 Agent 数据。
6. 每个阶段至少通过类型检查、构建检查和对应的交互检查。
7. 暂不扩展团队协作、插件市场、课程、题库和复杂白板。

## 3. 状态说明

- `[ ]` 未开始
- `[-]` 进行中
- `[x]` 已完成并通过验证
- `[!]` 阻塞，需要外部条件或用户决策

## 4. 总体阶段

### P0：产品急救与视觉地基

#### P0-1 响应式工作台

- [x] 修复窄屏登录页表单被压缩为 0 宽的问题。
- [x] 为工作台建立桌面、中等窗口、移动窗口三个布局层级。
- [x] 中等窗口自动折叠知识库，AI 助手改为覆盖式抽屉。
- [x] 小窗口只保留一个主内容区，知识库和 AI 使用抽屉打开。
- [x] 确保中间编辑器不会因侧栏固定宽度变成 0 宽。
- [x] 验证 1440、1024、768、634、390 像素宽度。

验收标准：

```text
1. 登录表单在所有目标宽度均可见、可输入、可提交。
2. 工作台中间区域在所有目标宽度均可访问。
3. 不出现横向页面溢出和不可关闭的遮挡层。
4. 桌面端原有三栏能力不退化。
```

#### P0-2 视觉系统

- [x] 统一背景、表面、主色、文字、边框、成功、警告、危险 Token。
- [x] 统一圆角、阴影、间距和交互动画。
- [x] 减少 11px/12px 正文字号，建立清晰字体层级。
- [x] 调整工作台外框，降低“所有区域都套卡片”的感觉。
- [x] 统一按钮、输入框、状态标签、空状态的视觉语言。
- [x] 校验浅色与深色主题对比度。

验收标准：核心页面具有一致的字号、间距、边框和状态色；浅色、深色主题均无明显可读性问题。

#### P0-3 工作台首页

- [x] 将“选择一篇笔记开始”替换为工作台首页。
- [x] 展示最近编辑笔记。
- [x] 展示未完成 AI 草稿和待确认修改。
- [x] 展示知识库与索引概况。
- [x] 提供新建笔记、快速记录、AI 创建三个主入口。
- [x] 提供适合新用户的首篇笔记引导。

验收标准：无选中笔记时不再是大片空白，用户可以从首页直接进入核心流程。

#### P0-4 搜索与版本

- [x] 左侧搜索接入后端全文/混合搜索，而不只过滤标题。
- [x] 搜索结果展示标题、分类、小节、命中片段和匹配方式。
- [x] 支持点击结果跳转笔记及来源位置。
- [x] 增加版本历史入口和版本列表。
- [x] 增加版本内容预览、差异说明和恢复确认。

验收标准：正文关键词可通过左侧搜索找到；用户可以查看并安全恢复历史版本。

### P1：基础笔记竞争力

#### P1-1 编辑器可靠性

- [x] 为现有复杂 Markdown 建立往返保真测试集。
- [x] 评估 Tiptap/ProseMirror 或其他成熟编辑器方案。
- [x] 验证标题、列表、表格、代码块、链接、引用、选区和撤销重做。
- [x] 验证 AI 修改后的选区、光标和 Markdown 内容稳定性。
- [x] 分阶段替换原生 `contentEditable + execCommand`（Tiptap 默认启用，超长不兼容内容自动安全降级）。

#### P1-2 知识组织

- [x] 补齐标签编辑和标签筛选。
- [x] 补齐收藏、最近打开和置顶视图。
- [x] 支持 `[[笔记名]]` 双向链接。
- [x] 增加反向链接面板。
- [x] 将已有“相关笔记”接口呈现在编辑器中。
- [x] 增加模板系统（4 个内置模板与创建流程）。

#### P1-3 快速捕捉与回顾

- [x] 增加快速收件箱。
- [x] 增加每日笔记（按日期幂等创建）。
- [x] 增加随机回顾或每日回顾卡片。
- [x] 支持把快速记录整理为正式笔记。
- [x] 支持由 AI 从碎片记录中发现主题和关联。

#### P1-4 附件与导入导出

- [x] 支持图片粘贴、拖拽上传和附件管理。
- [x] 支持 Markdown/ZIP 导入。
- [x] 完善 Markdown/ZIP/JSON 导出。
- [x] 为附件预留对象存储适配层。
- [x] 完成网页剪藏和 OCR 评估并明确后置，见 `docs/网页剪藏与OCR评估.md`。

### P2：AI 体验升级

#### P2-1 AI 上下文体验

- [x] 在 AI 面板顶部明确展示当前上下文范围。
- [x] 合并“聊天/问笔记”为统一输入和范围选择。
- [x] 来源卡片支持点击跳转原文并高亮。
- [x] 展示本次使用的笔记、记忆和检索范围。
- [x] 将内部工具轨迹转换为人类可读状态。

#### P2-2 草稿工作流

- [x] 将草稿界面改为“需求 → 大纲 → 分节正文 → 组装保存”步骤条。
- [x] 草稿模式自动降低无关侧栏干扰。
- [x] 优化小节生成、确认、返修、删除和恢复操作层级。
- [x] 增加保存完成反馈和新笔记定位。
- [x] 增加草稿跨刷新恢复。

#### P2-3 修改预览

- [x] 从左右全文对照升级为行级 Diff，并保留全文预览切换。
- [x] 高亮新增、删除内容，并显示新旧行号与统计。
- [x] 支持继续调整、撤销调整和恢复旧版本。
- [x] 将当前激活小节传入修改上下文。

### P3：后端工程与安全

#### P3-1 数据库与模块化

- [x] 引入 Alembic 数据库迁移。
- [x] 将启动时手写 `ALTER TABLE` 迁移为版本化脚本。
- [x] 拆分 Agent 路由中的规划、运行、Checkpoint、工具执行和记忆编排逻辑。
- [x] 拆分前端 workspace、editor、chat、draft、agent 状态访问层。
- [x] 补充统一错误响应 Schema、OpenAPI 契约和稳定错误码。

#### P3-2 认证安全

- [x] 将认证 Token 从 `localStorage` 迁移为安全 Cookie（旧会话已在重启后迁移为 HttpOnly Cookie，账户页可识别当前设备）。
- [x] 增加短期访问会话、刷新或服务端会话失效机制（服务端会话列表与当前设备状态已运行验收）。
- [x] 增加密码重置、登录限流和会话管理（生产通知通过环境 Webhook 接入）。
- [x] 将新密码迁移到 Argon2id，并兼容旧 PBKDF2 渐进升级。
- [x] 增加 CSP、安全响应头和生产环境 CORS 配置。

#### P3-3 异步任务

- [x] 将 Markdown 解析、Embedding 和重索引移入后台 Worker（真实笔记手动重建索引已由 Worker 完成）。
- [x] 保存笔记后快速返回，任务状态异步更新（任务提交和完成状态已在真实页面验收）。
- [x] 增加任务重试、失败原因和恢复能力（3 次指数退避与终态故障注入测试通过）。
- [x] 增加 Embedding 批处理和增量索引（沿用批处理并复用未变化 chunk 的 embedding）。
- [x] 统一索引任务与 Agent 任务状态展示（运行验收时补齐索引完成后的编辑器状态同步）。

#### P3-4 测试与可观测性

- [x] 扩展 API 集成测试（错误契约、安全头、认证、冲突、队列和真实数据库流程）。
- [x] 增加前端组件和 Store 测试。
- [x] 增加登录、笔记、搜索、草稿、修改、记忆的端到端测试。
- [x] 增加结构化日志、请求 ID、耗时和错误追踪。
- [x] 建立 RAG 质量、AI 成功率和取消/恢复成功率指标基础。

### P4：离线与多端（后续）

- [x] IndexedDB 本地草稿。
- [x] 断网编辑队列和重新联网同步。
- [x] 冲突检测与用户可见合并流程。
- [x] PWA 和移动端快速记录。
- [x] 完成桌面封装与本地文件同步评估，见 `docs/桌面封装与本地文件同步评估.md`。

## 5. 暂不执行范围

以下内容在核心体验稳定前不进入实施：

1. 团队实时协作。
2. 多租户企业权限。
3. 插件市场。
4. 完整无限白板。
5. 课程、题库、测验系统。
6. 社交社区和公开内容市场。

## 6. 通用完成定义

每个任务只有同时满足以下条件才标记为完成：

```text
[x] 功能代码已实现。
[x] 不破坏已有用户数据。
[x] TypeScript/Python 基础检查通过。
[x] 相关自动化测试通过。
[x] 对应页面完成桌面与窄屏验证。
[x] 加载、空状态、错误、重试已考虑。
[x] 本文档状态和实施记录已更新。
```

## 7. 实施记录

### 2026-07-15：任务书建立

已完成：

1. 完成前端、后端、设计文档和真实运行界面盘点。
2. 完成主流笔记产品能力对标。
3. 确定产品定位和 P0–P4 执行顺序。
4. 开始执行 P0-1 响应式工作台。

### 2026-07-15：P0-1 响应式工作台完成

已完成：

1. 登录页默认改为单列布局，只在大屏显示双列品牌区和表单区。
2. 1280px 以上保留可伸缩三栏工作台。
3. 768–1279px 使用 52px 知识库图标栏，AI 助手使用覆盖式抽屉。
4. 768px 以下只保留中间主工作区，知识库和 AI 均使用抽屉。
5. 增加移动端知识库与 AI 的可见入口、遮罩关闭、收起按钮和 Escape 关闭。
6. 选择移动端笔记后自动关闭知识库抽屉。

验证结果：

```text
npm run build：通过。
1440px：知识库 280px / 中间区 782px / AI 360px，无横向溢出。
1024px：中间区约 934px，无横向溢出。
768px：中间区 698px，无横向溢出。
634px：中间区 632px，无横向溢出。
390px：中间区 388px，无横向溢出。
634px 登录页：表单 360px，输入框 311px。
390px 登录页：表单 316px，输入框 267px。
浏览器控制台：无 error / warn。
```

### 2026-07-15：P0-2 视觉系统完成

已完成：

1. 浅色主题建立页面背景、侧栏表面、内容卡片三级层次；深色主题建立独立的背景、表面和卡片层次。
2. 增加成功、警告、危险语义 Token，清理登录、AI 草稿、AI 任务、记忆、删除确认中的主要硬编码状态色。
3. 建立应用外框、面板、抽屉、浮动入口、输入框、主按钮、次按钮、危险按钮、状态标签和空状态公共样式。
4. 统一焦点环、按压反馈、悬浮反馈、滚动条、选区和减少动画偏好。
5. 将知识库、账号菜单、AI 任务与草稿中的 11px 正文提升到 12px；关键操作提升到 13px，同时保留 11px 仅用于紧凑徽标。
6. 移动端外框移除多余圆角和阴影，桌面端使用单一应用级阴影，抽屉使用独立浮层层级。

验证结果：

```text
npm run build：通过（1802 modules transformed）。
浅色普通文字 / 白色：14.78:1。
浅色次要文字 / 白色：5.59:1。
浅色辅助文字 / 白色：4.70:1。
浅色主按钮：4.83:1；危险、警告、成功状态均不低于 4.90:1。
深色普通文字 / 卡片：13.76:1。
深色次要文字 / 卡片：8.78:1。
深色辅助文字 / 卡片：5.04:1。
深色主按钮：4.95:1；危险、警告、成功状态均不低于 5.98:1。
前端与后端开发服务已重新启动。
```

说明：本轮颜色对比度按 WCAG AA 普通文字 4.5:1 下限校验；响应式实机宽度已在 P0-1 完成验证，P0-2 未改变断点与内容区宽度逻辑。

下一步：执行 P0-3 工作台首页。

### 2026-07-15：P0-3 工作台首页完成

已完成：

1. 新增响应式工作台首页，替换无选中笔记时的单一空状态。
2. 最近编辑区按服务端 `updatedAt` 排序，展示目录、内容摘要和相对时间，点击直接打开笔记。
3. AI 待办区读取 Agent Checkpoint，区分草稿工作区和修改预览，并可直接继续处理。
4. 索引概况统计可检索、待更新、索引中和失败数量，展示可检索比例。
5. 新建笔记、快速记录、AI 创建三个入口均接入现有 Store；快速记录支持首行生成标题和快捷键保存。
6. 无笔记用户显示首篇笔记引导；加载失败显示错误信息和重新同步入口。
7. 知识库展开标题和折叠图标栏均增加工作台首页入口；首页选择状态可以持久化。
8. `FileNode` 补充 `updatedAt` 与 `summary`，编辑和服务端同步时持续更新最近编辑数据。

验证结果：

```text
npm run build：通过（1803 modules transformed）。
GET /api/health：healthy。
PostgreSQL：ok；Redis：ok；AI 配置：ok。
首页在单列、md 三入口、lg 主辅栏三个响应式层级下不会引入固定内容宽度。
```

下一步：执行 P0-4 搜索与版本。

### 2026-07-15：P0-4 搜索与版本完成

已完成：

1. 新增前端 `searchNotes` 服务，接入后端标题、小节、正文与向量混合检索接口。
2. 左侧搜索增加 260ms 防抖、取消过期请求、加载、空结果、错误和检索词改写反馈。
3. 搜索结果展示笔记标题、目录路径、小节、命中片段，以及标题、正文、语义组合匹配方式。
4. 点击搜索结果复用来源聚焦能力，自动打开笔记并滚动、高亮到对应小节或片段；移动端同步关闭知识库抽屉。
5. 编辑器元信息区增加版本历史入口；版本功能按需加载，避免拖大首屏包。
6. 版本抽屉支持列表、时间、来源、保存说明、历史正文预览和当前/历史行数差异摘要。
7. 恢复前使用确认弹窗说明影响；恢复时先保留当前内容，成功后同步 Store、正文、元数据和保存状态。
8. 新增可重复运行的 P0 API 冒烟脚本，临时创建测试笔记后验证搜索、版本生成和版本恢复，并在结束时删除测试笔记。

验证结果：

```text
npm run build：通过（1805 modules transformed）。
主包：435.82 kB；工作台首页：15.28 kB；版本面板：7.96 kB，均已拆分按需加载。
P0 API smoke：通过。
混合搜索：返回 4 条结果，检索通道包含 heading + content。
版本生成：1 条；版本恢复：通过；测试笔记：已删除。
```

下一步：进入 P1 基础笔记竞争力，先执行 P1-1 编辑器可靠性评估与保真测试。

### 2026-07-15：P1-1 编辑器可靠性第一阶段

已完成：

1. 新增 `scripts/editor-roundtrip.mjs`，以语义结构而非空格差异验证 Markdown 往返保真。
2. 覆盖标题、粗体、斜体、删除线、链接、有序/无序/嵌套列表、引用、GFM 表格、围栏代码块、下划线、高亮和字号 Span。
3. 修复现有编辑器保存表格时退化为普通文本的问题，增加表格列、对齐、单元格和换行序列化规则。
4. 完成 Tiptap 3、原生 ProseMirror、Lexical 官方能力对比，选定 Tiptap 3 作为目标方案。
5. 明确 Markdown 继续作为数据库、版本、RAG、AI 和导出的唯一正文契约；Tiptap JSON 只用于运行时。
6. 建立隔离试点、现有工作流接入、灰度切换和回退四阶段迁移门禁，详见 `docs/编辑器可靠性与迁移评估.md`。
7. 移动端编辑器不再常驻 300px 本文目录；目录改为抽屉，正文使用完整可用宽度，标题字号改为响应式。

验证结果：

```text
npm run test:editor-roundtrip：5 / 5 夹具通过。
npm run build：通过（1805 modules transformed）。
阶段记录：上述真实浏览器选区、撤销重做、中文输入法、粘贴与 AI 光标稳定性已在后续交互阶段补验。
```

下一步：继续 P1-1，增加真实编辑器交互测试，再开始 Tiptap 隔离试点。

### 2026-07-15：P1-1 真实交互验收与 Tiptap 隔离试点

已完成：

1. 将 Markdown 保真夹具从 5 组扩展到 8 组，新增任务列表、图片、转义字符、连续空行和混合 HTML。
2. 生产编辑器补齐任务列表和 `kbd` 元素序列化，避免复选框编辑保存后丢失。
3. 真实浏览器验证正文输入、选区浮动工具栏、纯文本粘贴、撤销、重做和 390px 移动端重排。
4. 新增 `TiptapPilotEditor`，只在开发环境通过 `?editor=tiptap` 显式加载；移除参数立即回退稳定编辑器。
5. 注册 Tiptap StarterKit、Markdown、Highlight、TextStyle、Table 和 TaskList 扩展，继续只保存 Markdown。
6. 实测粗体事务、任务列表、GFM 表格、撤销/重做，以及 Tiptap → 旧编辑器交叉打开。
7. 版本历史真实页面成功加载 10 个自动保存版本、差异摘要、内容预览和恢复入口。
8. Tiptap 独立拆为按需包；生产依赖漏洞为 0。审计发现的 Vite Windows 开发服务器问题已通过锁文件升级至 8.1.4 修复，完整审计为 0。
9. 新增共享 8 组编辑器夹具和 `test:tiptap-codec`，旧 Codec 与 Tiptap Codec 使用同一套门禁。
10. 新增 NoteFlow Tiptap 扩展，保留 `<u>`、`<mark>`、字号 `<span>`、`<kbd>`、图片、任务列表、GFM 表格和代码语言契约。
11. 将 Tiptap 拆为编辑器、Markdown、ProseMirror 三个按需块，并恢复独立 Store 块，消除构建大包警告。
12. AI 修改应用后增加一次性编辑器选区恢复请求；选择/小节/插入修改会重新选中新内容，整篇或删除修改会把光标恢复到正文开头。
13. 新增跨行内节点的 DOM Range 映射工具和 3 组自动测试，恢复后自动滚动并高亮目标段落。
14. 完成真实 AI 选区修改端到端验收：生成预览、应用、自动保存和重新索引均通过；应用后编辑器为激活状态，恢复选区非折叠、位于正文内，文本精确等于 AI 新内容。
15. 中文文本在真实 AI 输入框中无损保留；输入法提交判定覆盖 React 组合状态、原生 `isComposing`、兼容 keyCode 229、Shift+Enter 和正常 Enter 共 6 种路径。

验证结果：

```text
npm run test:editor-roundtrip：8 / 8 夹具通过。
npm run test:tiptap-codec：8 / 8 夹具通过，二次往返稳定。
npm run test:editor-selection：3 / 3 用例通过。
npm run test:input-composition：6 / 6 用例通过。
npm run build：通过（1869 modules transformed）。
主包：441.52 kB / gzip 130.29 kB。
Store：53.10 kB / gzip 14.15 kB。
Tiptap 试点：编辑器 114.30 kB、Markdown 345.28 kB、ProseMirror 50.12 kB，均仅显式试点时加载。
npm audit：0 vulnerabilities。
浏览器控制台 warn/error：0。
390px 旧编辑器：正文 353px，主区 390px，无横向溢出。
```

阶段记录：移动端软键盘属于具体设备专项回归；Tiptap 完整工作流与默认切换已在后续阶段完成。

下一步：继续 P1-1，将 Tiptap 接入大纲、版本、索引和 AI 选区恢复，完成灰度门禁后再进入 P1-2 知识组织。

### 2026-07-15：P1-1 完整工作流接入与灰度门禁

已完成：

1. Tiptap 灰度编辑器接入标题、大纲、自动保存、索引状态、重新索引、版本历史和移动端目录抽屉。
2. 所有格式操作改用 Tiptap/ProseMirror Transaction 与节点命令，不再依赖 `execCommand`。
3. AI 选中内容和来源跳转接入 ProseMirror Range；修复 emoji 的 UTF-16 位置偏移风险。
4. 新增 `test:tiptap-selection`，中文、跨行内格式、emoji 与未命中回退共 4 组通过。
5. 新增只读历史语料导出和 `test:tiptap-history`；任何语义签名变化或第二次往返不稳定都会阻止默认切换。

验证结果：

```text
npm run test:tiptap-selection：4 / 4 通过。
npm run test:tiptap-codec：8 / 8 通过。
npm run build：通过（1870 modules transformed）。
历史审计当时受限，后续已完成 27 条只读审计并实现自动安全降级。
Tiptap AI 回填当时受限，后续真实页面与 E2E 已通过。
```

阶段结论：当时保持灰度；最终审计通过后已切换为默认编辑器。

### 2026-07-15：P1-2 知识组织实现

已实现：

1. 笔记标签可新增、删除并持久化；知识库可按标签筛选。
2. 左侧新增全部、置顶、收藏、最近四种视图；最近视图同时参考最近打开和更新时间。
3. 新增 `[[笔记名]]`、`[[笔记名#小节|显示名]]` 解析器，忽略行内代码和围栏代码块。
4. 编辑器底部新增链接到、反向链接和智能相关三栏；Wiki 目标与后端相关笔记均可跳转。
5. 新增会议记录、项目主页、阅读笔记和决策记录 4 个模板，支持日期、标题和默认标签实例化。
6. 修复正文更新时用空数组/false 覆盖既有标签与收藏状态的问题。

验证结果：

```text
npm run test:wiki-links：解析 2 个有效链接、忽略 2 个代码链接、反链 1 / 1 通过。
npm run build：通过（1874 modules transformed）。
主包：452.07 kB / gzip 133.43 kB；模板工作台按需块：20.02 kB / gzip 5.80 kB。
桌面与 390px 页面验收已在最终浏览器阶段补验。
```

下一步：在不依赖页面控制的前提下执行 P1-3 快速捕捉与回顾；P1-2 保持“已实现、待页面验收”。

### 2026-07-15：P1-3 快速捕捉与回顾实现

已实现：

1. 快速记录自动写入“收件箱”标签，并在工作台集中展示待整理碎片。
2. “移出收件箱并整理”会去掉收件箱标签并打开正式编辑器。
3. 今日笔记以 `YYYY-MM-DD 每日笔记` 幂等创建，包含今日重点、随手记录和复盘结构。
4. 每日回顾按日期稳定推荐一篇，也可随机换一篇。
5. “AI 发现主题”以问笔记模式检索收件箱标签和正文，要求输出主题、关联、整理建议和来源。

验证结果：

```text
npm run build：通过（1874 modules transformed）。
工作台按需块：26.08 kB / gzip 7.17 kB。
页面交互与真实 AI 流式回答已在最终浏览器/E2E 阶段补验。
```

### 2026-07-15：P1-4 附件与导入导出实现

已实现：

1. 旧编辑器和 Tiptap 灰度编辑器均接入图片/文件粘贴、正文拖拽和附件面板拖拽上传。
2. 新增附件列表、大小展示、删除和插入正文；默认单文件上限 10 MB，拒绝主动 HTML/SVG。
3. 新增受 HMAC 保护的附件内容 URL，正文不嵌入大体积 data URL，也不会污染 RAG 分块。
4. 新增 `ObjectStorage` 接口和本地文件适配器，S3/R2/OSS 只需替换 put/get/delete 实现。
5. 新增 Markdown、Markdown ZIP 和 NoteFlow JSON 导入；ZIP 支持 Store/Deflate、路径净化、文件数和体积限制。
6. 账号菜单补齐 Markdown ZIP 与 NoteFlow JSON 导出；单篇 Markdown 导出继续保留。

验证结果：

```text
npm run test:knowledge-import：ZIP 2 / 2 Markdown 文件往返通过。
python unittest：15 / 15 通过，其中对象存储 put/get/delete/路径逃逸门禁通过。
python compileall：通过。
npm run build：通过（1877 modules transformed）。
后端附件 API 与对象存储契约已在最终运行和测试阶段补验。
桌面与窄屏页面已在最终浏览器阶段补验。
```

阶段推进：进入 P2；P1-2～P1-4 的运行态验收已在最终阶段补齐。

### 2026-07-15：P2 AI 体验升级实现

已实现：

1. AI 面板合并聊天与问笔记入口，使用“自动 / 当前笔记 / 知识库 / 已选内容”统一范围选择，并展示上下文回执。
2. 来源卡片继续支持跳转与原文高亮；检索笔记数、片段数、通道、记忆使用情况和工具轨迹均转换为可读信息。
3. AI 草稿新增“需求 → 大纲 → 分节正文 → 组装保存”步骤条；草稿和修改预览进入专注模式，隐藏无关知识库与 AI 侧栏。
4. Agent Checkpoint 绑定持久化草稿 ID，刷新后可恢复未完成草稿；正式保存后定位新笔记并显示完成反馈。
5. 修改预览默认升级为行级 Diff，显示旧/新行号、增删颜色与统计，同时保留左右全文预览。
6. 新增服务端预览修订记录，支持撤销上次调整和选择恢复任一旧版；正式笔记仍须用户确认后才写回。
7. 稳定编辑器和 Tiptap 根据当前活动标题映射后端小节 ID，并随 AI 请求传入修改上下文。

验证结果：

```text
npm run build：通过（1878 modules transformed）。
npm run test:markdown-diff：通过（新增 2 行、删除 1 行、6 行对比）。
npm run test:input-composition：6 / 6 通过。
python unittest：16 / 16 通过，包含预览修订快照测试。
python compileall：通过（缓存目录定向到可写临时目录）。
页面与真实 AI/Checkpoint/修订 API 已在最终 E2E 阶段补验。
```

阶段推进：进入 P3；P2 运行态条目已在最终 E2E 阶段补齐。

### 2026-07-15：P3 后端工程、安全与可观测性实现

已实现：

1. 引入 Alembic，建立 `0001` 基线、服务端会话、密码重置、索引重试共 4 个顺序迁移；启动时不再手写 `ALTER TABLE`。
2. Agent 的请求 Schema、运行生命周期、上下文规划和 Checkpoint 读写已拆分到独立模块；记忆工具编排随后也迁至独立服务。
3. 全局 API 错误统一为 `{ error: { code, message }, requestId }`，增加请求 ID、结构化耗时日志、安全头和内存指标快照。
4. 认证改为 HttpOnly Cookie；访问 JWT 30 分钟、服务端会话 7 天，可刷新、逐设备查看和撤销。旧 Bearer Token 只用于一次迁移，成功后从浏览器存储删除。
5. 新密码使用 Argon2id；旧 PBKDF2 登录成功后自动升级。新增登录/注册/重置限流、一次性密码重置令牌和生产通知 Webhook。
6. Markdown 解析、切块与 Embedding 移入持久化后台 Worker；快速连续保存复用 pending 任务，失败最多 3 次指数退避。
7. 沿用 Embedding 批处理与 content hash 增量复用；AI 面板统一显示索引和 Agent 任务状态。

验证结果：

```text
Alembic head：20260715_0004。
python compileall：通过。
python unittest：26 / 26 通过。
npm run build：通过（1879 modules transformed）。
生产后端迁移、Cookie、Worker、响应头已在后续受控重启中完成运行验收。
```

### 2026-07-15：P4 离线与多端基础实现

已实现：

1. 正文网络保存失败时写入按用户/笔记去重的 IndexedDB 队列，并明确显示“离线已保存”。
2. 浏览器恢复联网后自动同步；请求携带远端版本时间，避免静默覆盖其他设备的更新。
3. 检测到冲突时保留远端正式笔记，并把本地内容创建为带“同步冲突”标签的副本。
4. 工作台新增同步冲突处理区，可打开原笔记、冲突副本并在人工合并后标记完成。
5. 新增 Web App Manifest、离线壳 Service Worker 和 standalone PWA 配置；移动端既有快速记录可从安装后的应用直接使用。

验证结果：

```text
npm run test:offline-conflict：通过。
前端生产构建及 9 组编辑器、输入、导入、Wiki、Diff、离线测试全部通过。
本阶段原始记录：相关页面验收当时受限；真实断网恢复、双设备冲突与 PWA 安装资源已在后续阶段全部补验。
```

### 2026-07-15：解除运行阻塞并完成首轮真实验收

处理结果：

1. 正常停止占用 `5173/8080` 的旧开发进程，使用统一 `npm run dev` 启动新前后端。
2. 启动时顺序应用 `20260715_0001` 至 `20260715_0004`，数据库当前版本确认为 `20260715_0004 (head)`。
3. 健康检查确认 PostgreSQL、Redis 和 AI 配置可用；响应携带请求 ID、CSP、禁止嵌入、内容类型保护等安全头。
4. 浏览器控制恢复后完成 `1440/1024/768/634/390` 五档页面检查，均无横向溢出，中间编辑区保持可用。
5. 旧登录态已迁移到服务端 Cookie 会话，账户页能显示 Chrome/macOS 当前设备与服务端会话记录。
6. 真实触发笔记重建索引，后台 Worker 完成处理；同时修复了任务成功后编辑器仍显示“待更新”的状态同步问题。
7. 使用生产预览验证 Service Worker：停止页面服务器后仍能加载应用壳。首次测试发现离线刷新会退回登录页，随后增加不含 Token 的限时用户资料缓存；复测后可离线重新进入本机缓存工作台。
8. 离线工作台的网络错误文案改为“正在使用本机缓存”，保留重新同步入口。

验证结果：

```text
dev ports：frontend 5173 / api 8080，均正常。
GET /api/health：200，PostgreSQL/Redis/AI 检查通过，安全响应头生效。
Alembic current：20260715_0004 (head)。
真实后台索引：待更新 → 已完成。
响应式布局：五档宽度均 horizontalOverflow=false。
PWA 离线重载：服务器停止后仍进入缓存工作台并显示本地笔记。
npm run build：通过（1879 modules transformed）。
```

### 2026-07-15：最终工程收口与交付验收

完成结果：

1. 前端建立 workspace、editor、chat、draft、agent 五个状态访问分片；业务组件不再订阅整棵 Store。
2. Agent 记忆查询、保存、上下文、历史回忆和情节记忆编排迁至独立服务；主路由由 1901 行降低到约 1330 行。
3. 新增前端 Store/组件运行测试，覆盖 LoginPage、NoteMetadataControls、WorkbenchHome 和 EditPreviewWorkspace，共 23 项断言。
4. 新增真实运行 E2E：Cookie 注册登录、真实 AI SSE 与工具轨迹、笔记/索引、混合搜索、草稿确认组装保存、修改预览/修订/应用、记忆 CRUD、会话刷新和双设备版本冲突均通过；测试账户自动清理。
5. 统一错误 Schema 写入 OpenAPI，错误继续使用 `{ error: { code, message }, requestId }`；补充 Agent 记忆单测和索引失败 3 次指数退避/终态故障注入测试。
6. Tiptap 成为默认编辑器；历史只读审计覆盖 5 篇笔记和 22 个版本，25 条直接通过，2 条超长语料由 40,000 字符保真门禁自动回退经典编辑器，`blockedCount=0`。
7. 补齐 192×192、512×512 与 Apple Touch PNG 图标；Manifest、standalone、Service Worker 离线资源和实际 HTTP Content-Type 验收通过。
8. 实际浏览器确认默认新版编辑器、手动经典回退、标签、附件、关联和 AI 上下文；390px/1280px 无横向溢出，控制台无 error/warn。
9. 完成桌面封装/本地文件同步、网页剪藏/OCR 评估，明确后续启动门槛和数据安全边界。
10. 最终前后端再次受控重启，健康检查、Cookie、安全响应头、PostgreSQL、Redis、AI 配置和 Alembic head 正常。

最终验证结果：

```text
npm run build：通过（1888 modules transformed）。
前端组件/Store：10 个 Store 断言 + 13 个组件断言通过。
编辑器：旧/新 Codec、选区、输入法、历史语料与兼容回退全部通过。
知识功能：Wiki、ZIP 导入、行级 Diff、离线冲突测试通过。
PWA：standalone、192/512 PNG、离线壳资源测试通过；实际资源均 HTTP 200。
Python compileall：通过（缓存写入 /private/tmp）。
Python unittest：31 / 31 通过。
运行 E2E：login、agent、note/index、search、draft、edit、memory、multi-device conflict、refresh 全部通过。
Alembic current/heads：20260715_0004 (head)。
GET /api/health：200；PostgreSQL、Redis、DeepSeek 配置正常。
浏览器：390px / 1280px horizontalOverflow=false；console error/warn=0。
```

交付结论：本任务书范围全部完成。后续若继续迭代，应以实际用户数据决定剪藏/OCR、桌面封装、原生文件同步或团队协作的优先级，而不是作为本轮遗留项继续扩张。

# NoteFlow 开发问题全量复盘

> 整理日期：2026-09-13
> 整理范围：Codex 当前 `NoteFlow` 工作区（`/Users/zhangcheng/Desktop/NoteFlow`）关联的全部可见任务  
> 目的：把开发中实际遇到的问题、根因、处理结果、遗留风险和对应提交集中到一个可持续维护的入口。

## 1. 信息来源与口径

本次读取并逐回合检查了以下两条 Codex 任务，共 80 个回合：

| 任务 | 回合数 | 主要内容 |
| --- | ---: | --- |
| `完善登录功能体验` | 70 | 登录、邮箱验证码、密码重置、账号设置、本地环境、阿里云购买与部署 |
| `优化整体 UI 设计体验` | 10 | 全局 UI 审计、四轮设计稿、AI 草稿修复、生产配置、Git 推送与迁移说明 |

当前归档任务中没有属于该 NoteFlow 工作区的对话。问题按主题去重归并；用户针对同一问题的多次反馈会合并为一项，但会保留方案反复、失败重试和方向纠偏等关键过程。

最终归并出 69 项问题与决策记录，覆盖认证、账号信息架构、主工作台、AI 草稿、开发测试、阿里云运维以及 Git/交付七个类别。

状态说明：

- ✅ 已解决：代码或配置已落地，并有构建、测试或运行验证。
- 🟡 部分解决：已有设计、配置或临时处理，但最终运行结果仍未完全闭环。
- 🔴 未解决：已确认问题，但尚未进入正式产品代码或尚未完成上线验证。
- ⚪ 已澄清：属于认知、决策或操作问题，已给出明确口径，不代表需要改代码。

> 注意：对话是过程证据，仓库代码与 Git 提交是最终事实。本文不记录授权码、密码、私钥等秘密；邮箱、IP 等个人部署信息也不展开复述。

## 2. 总体结论

整个开发阶段的问题并不是单一的“页面不好看”，而是五条线同时暴露：

1. 认证最初只有基础邮箱密码表单，验证、找回、迁移和真实邮件链路不完整。
2. 账号、设置、邮箱、设备、数据管理曾堆在同一个小菜单里，信息架构反复调整。
3. 主工作台功能完整，但缺少统一设计系统，正文、AI 面板、草稿和修改预览像不同产品。
4. 本地依赖、预览进程、Docker 镜像网络和低配服务器使“代码完成”多次不等于“环境可用”。
5. 设计探索一度追求新颖而偏离真实产品；后续才恢复“先映射现有功能，再设计视觉语言”的方法。

当前最大的未闭环项有三个：**新版工作台仍停留在设计稿、阿里云生产部署未完成最终运行验收、现有数据尚未形成跨机器迁移包**。

## 3. 登录、认证与账号问题

| ID | 问题与现象 | 根因 | 处理与当前状态 | 状态 |
| --- | --- | --- | --- | --- |
| AUTH-01 | 初版登录页粗糙，左侧强调技术栈，表单层级弱，缺少密码显隐、Caps Lock、强度与完整反馈。 | 页面以功能可用为目标，没有按真实产品首屏设计。 | 重做双栏登录页并补齐交互、移动端、深色模式、焦点与无障碍；前端构建和组件测试通过。对应 `b9c9dd1`。 | ✅ |
| AUTH-02 | 任意格式合法的“假邮箱”也能注册，用户无法理解邮箱的真实用途。 | 当时没有邮箱所有权验证。 | 增加邮箱 6 位验证码登录；首次验证自动建号，已有邮箱匹配原账号，密码登录继续保留。 | ✅ |
| AUTH-03 | 登录方案在账号密码、通行密钥、手机号验证码和邮箱验证码之间反复讨论。 | 自部署、国内使用、上线成本与最佳体验目标没有先形成约束。 | 最终选择“邮箱验证码为主、邮箱密码备用”；手机号和 Passkey 暂不进入当前版本。 | ⚪ |
| AUTH-04 | 旧账号担心迁移后产生重复用户或丢失笔记。 | 新增验证码登录时必须兼容既有 `user_id` 和未验证邮箱。 | 数据库迁移保留原用户 ID、密码和知识库；真实旧邮箱可直接验证认回，假邮箱可登录后改绑。迁移前做过数据库备份。 | ✅ |
| AUTH-05 | 本地页面提示“开发环境验证码已自动填入”，让真实邮件能力看起来像假的。 | `development` 环境未配置 SMTP/Webhook 时启用开发验证码兜底。 | 本地接入 SMTP 后真实验证码链路跑通；生产仍依赖部署机正确配置 SMTP 密钥。 | 🟡 |
| AUTH-06 | 用户混淆“系统发件邮箱”和“NoteFlow 登录账号”，期间还出现邮箱地址写错。 | UI 和沟通中没有先把 sender identity 与 account identity 分开。 | 已明确二者独立：固定发件邮箱只负责投递，用户登录邮箱对应数据库账号；配置时对授权码做了不回显检查。 | ⚪ |
| AUTH-07 | 找回密码看似可用，实际 SMTP 未接入；失败时还会把开发令牌交给前端，形成“假重置”。 | 重置模块沿用旧 Webhook/开发令牌路径，没有复用真实 SMTP 投递。 | 补上真实邮件发送、一次性令牌、过期和旧会话注销，并删除前端开发令牌直达流程；随后进一步改为验证码重置。 | ✅ |
| AUTH-08 | 邮件重置链接使用 `127.0.0.1:5173`，在手机或其他电脑打开必然失败。 | 本地服务地址只对运行前端的电脑有效，尚无公网域名。 | 找回密码改为“邮件 6 位验证码 + 新密码”，手机收码、电脑输入，不再依赖本地链接。 | ✅ |
| AUTH-09 | 找回不存在的邮箱时，页面统一提示会让用户觉得没发邮件；改为明确 404 后又带来账号枚举风险。 | 可理解性与隐私安全目标冲突。 | 当前开发环境会明确提示“该邮箱还没有账号”，生产环境仍返回统一消息。应保留此环境分支并补安全回归测试。 | 🟡 |
| AUTH-10 | 登录页解释文字过多，包括自动注册、旧账号迁移和重复产品文案。 | 把帮助说明直接堆到首要操作界面。 | 删除宣传和重复解释，仅保留品牌、方式切换、必要字段、操作按钮及状态反馈。 | ✅ |
| AUTH-11 | 登录邮箱修改后，其他设备可能继续保持旧会话。 | 账号标识变化属于高风险安全事件。 | 修改邮箱成功后撤销其他会话；设备管理仍保留在账号安全能力中。 | ✅ |

## 4. 账号入口与设置信息架构

| ID | 问题与现象 | 根因 | 处理与当前状态 | 状态 |
| --- | --- | --- | --- | --- |
| IA-01 | 左下角账号入口使用数字头像，用户名和邮箱重复，像临时占位。 | 没有正式的身份展示规则。 | 数字头像改为统一账号图标；昵称与邮箱重复时去重。 | ✅ |
| IA-02 | 用户不希望邮箱长期显示在侧栏，存在隐私和视觉噪声。 | 把邮箱同时当作登录标识和公开昵称。 | 侧栏及菜单顶部默认只显示昵称或“我的账号”，邮箱只在登录邮箱设置中出现。 | ✅ |
| IA-03 | 账号入口“卡片套卡片”，边框、头像框和阴影过重。 | 侧栏已有容器边界，入口又重复使用完整卡片语言。 | 改为轻量账号行，默认融入侧栏，只在悬停或展开时出现背景反馈。 | ✅ |
| IA-04 | 邮箱、设备、外观、导入导出、记忆和回收站全部挤在账号菜单，层级混乱。 | “我是谁”和“软件如何工作”没有分离。 | 先分组为账号安全、显示、数据、记忆、回收站，随后进一步拆分账号与设置。 | ✅ |
| IA-05 | 拆分后的“回收站 / 设置 / 我的账号”三行仍显笨重，设置弹窗像后台管理。 | 结构虽然正确，但入口数量和弹窗说明仍过多。 | 左下角收成单行“账号 + 回收站图标 + 设置图标”；设置使用顶部标签并随内容自适应高度。 | ✅ |
| IA-06 | 设置到底放左下角、账号菜单内还是右上角，方案反复。 | 没先按频率、语义和编辑器空间做定位原则。 | 当前原则：低频入口留在侧栏底部；账号只管身份与安全，设置管通用、AI/记忆和数据。 | ⚪ |

## 5. 主工作台视觉与交互问题

这些问题来自完整 UI 审计。除特别说明外，绝大多数还没有进入正式业务代码；设计稿不等于功能已改造。

| ID | 问题与现象 | 根因 | 建议/处理与当前状态 | 状态 |
| --- | --- | --- | --- | --- |
| UI-01 | 全局大量“白底 + 细边框 + 圆角矩形”，出现卡片套卡片和十几年前后台感。 | 缺少统一表面层级，组件各自用 Tailwind 临时组合。 | V4 设计稿已降低侧栏与 AI 面板对比，把正文设为最高层级；正式代码未改。 | 🔴 |
| UI-02 | 文字普遍只有 11–13px，信息碎、密、扫描路径弱。 | 字号和间距按局部塞内容，而不是按全局阅读节奏设计。 | 已在设计稿中建立更清晰的标题、正文、元信息层级；待形成正式 Typography Token。 | 🔴 |
| UI-03 | 左栏、中间正文和右侧 AI 视觉权重接近，正文不是主角。 | 三栏都使用相似背景、边框和密度。 | 设计稿明确“正文最高、两侧降噪”，尚未编码。 | 🔴 |
| UI-04 | 同屏重复使用蓝色浅底状态、描边按钮和胶囊标签，状态含义不清。 | 品牌色、状态色和动作色没有语义分工。 | V4 已收敛关键色；仍需在正式 Token 和基础组件层实施。 | 🔴 |
| UI-05 | CSS 混用设计 Token、Tailwind 与大量临时十六进制颜色，深色模式被浅色硬编码打断。 | 样式演进缺少强制设计系统和暗色语义变量。 | 审计已定位，尚未统一。 | 🔴 |
| UI-06 | `NoteEditor`、`AIPanel`、`AIDraftWorkspace`、`EditPreviewWorkspace` 像不同产品。 | 各功能分阶段独立实现，没有共享标题、表面、按钮和状态规范。 | V4 先映射真实组件；草稿和预览仍需单独设计、逐模块实施。 | 🔴 |
| UI-07 | 桌面端 AI 面板原默认 350px 太窄，连续调到 420px、500px。 | 只以聊天内容舒适度调宽，没有同步建立正文最小宽度策略。 | 当前默认 500px、可拖拽 320–680px；但 1280px 屏上正文可能只剩约 464px，应增加自适应折叠或覆盖模式。 | 🟡 |
| UI-08 | 文档标题可能在顶部标题输入框和正文首个同名 H1 中重复。 | 导入/渲染没有统一“文档标题”与“正文一级标题”契约。 | 已列为 P0 设计问题，尚未实现自动剥离或去重。 | 🔴 |
| UI-09 | AI 修改预览像数据库 Diff 工具，普通用户难以审阅。 | 默认以技术差异而不是最终阅读结果为中心。 | 建议默认展示修改后文档并行内高亮，Diff 降为高级视图；尚未实现。 | 🔴 |
| UI-10 | 搜索弹窗固定约 800×680，但初始态内容很少，空且笨重。 | 初始搜索与结果浏览共用同一大容器。 | 建议初始为紧凑 Command Palette，有结果后再扩展；尚未实现。 | 🔴 |
| UI-11 | AI 草稿工具栏拥挤、正文区空、标题重复，像独立后台。 | 草稿规划、生成、控制和编辑没有按阶段重新组织。 | 建议演进为创作 Studio，并复用主工作台设计语言；尚未进行完整 UI 改造。 | 🔴 |
| UI-12 | 手机端顶部悬浮按钮挤压标题，时间信息换行。 | 桌面工具按钮直接压缩到窄屏，没有移动端顶栏。 | 建议建立固定移动顶栏，统一返回、目录、标题和更多操作；尚未实现。 | 🔴 |
| UI-13 | 首版设计稿虽整洁，仍是传统“侧栏 + 纸张卡片 + 聊天栏”。 | 只做视觉装修，没有改变信息层级和 AI 协作方式。 | 产生 V2 AI 原生方向稿，用浮动工具轨、无边界画布和协作层探索替代方案。 | ⚪ |
| UI-14 | V2/V3 为追求新颖进一步偏离 NoteFlow，真实目录、编辑器、引用、草稿和修改预览能力丢失。 | 先追趋势后理解产品，设计没有建立组件与功能映射。 | 承认方向错误，重新审计 `DirectoryTree / NoteEditor / AIPanel`，V4 恢复真实三栏和功能映射。对应 `4ca7960`。 | ✅ |
| UI-15 | 用户需要“一张完整可评审的高保真图片”，此前内置图像服务连续两次网络失败。 | 外部生成服务不稳定，且位图生成对复杂中文 UI 的可控性有限。 | 2026-09-09 已成功生成并保存 V5 完整 PNG，三栏与真实功能映射通过人工检查；最终画风仍待用户确认，因此不能标记为完全解决。 | 🟡 |
| UI-16 | 动效、图标、排版曾停留在“加渐变/阴影”的表层讨论。 | 没有先定义图标网格、状态语义、动效目的和内容层/控制层关系。 | V3 研究稿补了图标网格、排版与状态动效原则，但其产品结构不采用；原则可复用到 V4 实施。 | 🟡 |
| UI-17 | V5 大体结构可接受，但图标、控件轮廓、阴影和状态细节仍偏硬、偏通用，没有达到“柔滑、时尚”的美感。 | V5 主要解决信息层级和卡片嵌套，尚未对图标光学重量、连续曲率、材质和动效曲线建立完整系统。 | V6 建立圆端细线图标、连续曲率、宽而淡的环境阴影和柔和过渡，V8 进一步以确定性组件完整落地；用户已认可 V8 整体 UI 风格。 | ✅ |
| UI-18 | 用户能够判断方案仍显老土，但仅凭“柔滑、时尚、美观”无法准确表达目标，导致多轮设计仍在相近的三栏 SaaS 语法内微调。 | 设计前没有把审美偏好拆成气质、控件、重构范围、色调、图标和信息密度等可选择维度。 | 已通过两组选择题确认 `BBC + CBB` 偏好矩阵。V8 延续该矩阵并改用精确渲染，用户已明确认可整体 UI 风格；后续不再重新选择画风。 | ✅ |
| UI-19 | V7 完整图片中的文字、图标、间距和边缘出现歪曲、漂移与不一致，整体看起来杂乱，无法作为精密 UI 的评审稿。 | 使用生成式位图模型直接绘制复杂中文界面；模型按像素近似文字与几何，无法保证基线、网格和重复组件的一致性。 | 停止用生成式位图碰运气，V8 改用真实 HTML/CSS、规则网格与同一套 Lucide 矢量图标搭建，再由浏览器渲染 1586×992 PNG；人工检查和用户后续的整体风格认可均未再出现生成畸变反馈。 | ✅ |
| UI-20 | 用户认可 V8 的整体 UI 风格，但明确指出很多功能是设计者凭空增加或按自己的想法重组，部分元素没有实际意义、可有可无，当前方案不能直接进入代码实施。 | 设计过程先画“理想工作台”，再把功能填入版面；没有先从现有代码与真实界面建立不可遗漏的功能、状态、入口、命名和上下文归属清单，也没有执行“每个元素都必须有真实用途”的必要性检查。 | 已逐项审计 `App.tsx`、目录、编辑器、AI、草稿、修改预览和全库搜索，形成 `design/noteflow-current-function-audit.md`；V9 删除 V8 的虚构顶栏、快捷分区、统计、AI 模式与任务状态，只呈现当前真实可达主工作台。浏览器已渲染并人工检查完整 PNG，仍待用户确认功能位置后才能进入代码实施。 | 🟡 |
| UI-21 | 代码目录中存在完整格式工具栏、版本历史、元数据、附件、笔记关系、语音识别和 Agent 任务面板等实现，但当前主界面没有入口或被 `false &&` 禁用；仅按组件名审计会误判为现有可见功能。 | “组件已实现”和“用户当前可达”没有建立统一口径，历史设计稿据此把未接入能力画成了常驻功能。 | V9 审计改为按“真实入口 → 状态触发 → 业务调用 → 数据来源”四步判断；这些不可达能力已从主稿移除并在功能审计中单列。是否接回入口属于后续独立产品决策，当前未修改代码。 | 🔴 |

## 6. AI 草稿与编辑状态问题

| ID | 问题与现象 | 根因 | 处理与当前状态 | 状态 |
| --- | --- | --- | --- | --- |
| AI-01 | 用户停止章节生成后，已经生成的部分正文可能丢失。 | 停止逻辑只处理终止，没有把流式部分结果落回章节。 | 停止时保留已有正文。对应 `2e8e8e7`。 | ✅ |
| AI-02 | 停止后章节状态不准确，可能回退成未生成而不是保留可继续编辑的状态。 | 状态回写没有根据部分正文是否存在分支处理。 | 根据部分正文决定章节最终状态，并给出明确停止反馈。 | ✅ |
| AI-03 | 关闭草稿工作区后，已建立的草稿上下文可能回退。 | UI 关闭动作与业务草稿生命周期耦合。 | 保留草稿上下文，避免单纯关闭界面造成状态倒退。 | ✅ |
| AI-04 | 章节动作文案在生成、编辑、确认之间不统一。 | 不同状态分别追加按钮，缺少动作词规范。 | 精简并统一章节操作文案。 | ✅ |
| AI-05 | 停止、恢复、部分结果的修复只跑过前端测试和构建，缺少完整浏览器到后端 Worker 的 E2E 证据。 | 本轮提交主要改前端状态协调。 | 需要补“生成中停止 → 刷新 → 恢复/编辑 → 保存”的端到端回归。 | 🟡 |

## 7. 本地开发、预览与测试问题

| ID | 问题与现象 | 根因 | 处理与当前状态 | 状态 |
| --- | --- | --- | --- | --- |
| DEV-01 | 5173 预览刚能访问，交付时又打不开。 | 临时 Vite 进程在清理阶段被停止，用户却仍依赖该地址验收。 | 当时重新启动并确认 `200 OK`；以后应在结束前明确“预览是否保持运行”。 | ⚪ |
| DEV-02 | `npm run dev` 无法完整启动本地前后端。 | 仓库要求 Python 3.12+，但项目 `.venv` 不存在，启动脚本落到未安装 `uvicorn` 的解释器；同时本地 PostgreSQL 和 Redis 依赖 Docker Desktop。 | 2026-09-13 使用本机 Python 3.12 创建被 Git 忽略的 `.venv`，按 `server/requirements.txt` 安装全部后端依赖；启动 Docker Desktop 与 PostgreSQL/Redis 后重新执行 `npm run dev`。Vite 在 `127.0.0.1:5173` 返回 200，`/api/health` 返回 200 且 PostgreSQL、Redis、AI 均 ready，完整开发环境已实际运行。 | ✅ |
| DEV-03 | 后端认证专项测试一度因缺少 `httpx` 无法启动。 | 测试依赖没有被当前 Python 环境完整安装。 | 后续使用项目后端测试环境跑通相关认证用例，但新机器仍需按依赖清单创建环境。 | 🟡 |
| DEV-04 | Python 字节码缓存曾被运行环境限制，造成检查命令失败。 | 测试命令默认尝试写缓存，而执行环境不允许相应路径。 | 改用项目运行环境/禁写字节码方式继续验证，不属于业务代码错误。 | ⚪ |
| DEV-05 | 前端契约测试仍要求旧登录页默认展示“忘记密码/注册”，与验证码默认模式冲突。 | UI 已变更，测试基线未同步。 | 更新过期断言后重新跑通组件和编辑器界面测试。 | ✅ |
| DEV-06 | 多次启动 `dev:vite --strictPort` 失败。 | 5173 端口已被既有预览进程占用或进程状态不清。 | 使用现有进程或重启后继续；应增加统一的预览状态检查脚本。 | 🟡 |
| DEV-07 | 生成复杂 UI 位图时外部图像服务曾两轮网络失败，随后生成成功也无法保证中文和几何精度。 | 依赖外部服务且请求耗时长；生成式位图本身不适合承担精密 UI 的文字和网格绘制。 | V8 固定采用 HTML/CSS + 矢量图标 + 浏览器截图的确定性流程，完整 PNG 不再依赖外部生成服务；后续仅在需要氛围插画或纹理时使用图像模型。 | ✅ |
| DEV-08 | 本地重建后端镜像时基础镜像下载卡住。 | Docker Hub 网络不稳定。 | 一次采用向现有容器更新代码的临时方式完成验证，但该方式不应代替可重复镜像构建。 | 🟡 |
| DEV-09 | 本地启动原项目时，Docker Desktop 自动恢复的旧 `noteflow-backend` 容器占用了 8080；第一次先启动应用、后启动数据库也使后端等待超时退出。 | 容器使用 `restart: unless-stopped`，Docker 启动后会恢复旧服务；开发脚本虽然支持端口避让，但基础服务启动顺序仍需满足。 | 先启动 Docker，再确认 PostgreSQL/Redis 健康，随后重新运行开发脚本；脚本检测到 8080 被占用后自动绑定 8081，并把 Vite 代理同步到 8081。当前前端、开发后端和基础服务均正常，未删除或覆盖旧容器。 | ✅ |

## 8. 阿里云部署与运维问题

| ID | 问题与现象 | 根因 | 处理与当前状态 | 状态 |
| --- | --- | --- | --- | --- |
| OPS-01 | 服务器选型先推荐轻量 2C4G，和“只要最低成本”的真实预算冲突。 | 先按稳妥生产规格判断，没有先确认成本优先级。 | 重新比较轻量与 ECS，最终选择低价 2C2G 年付 ECS，并通过 Swap/资源限制补偿。 | ⚪ |
| OPS-02 | 新服务器只有约 1.6GB 可用内存且没有 Swap，同时运行 PostgreSQL、Redis、后端和前端风险高。 | 2GB 套餐资源紧张。 | 服务器创建 2GB Swap；生产 Compose 增加容器内存限制。仍需上线后观察 OOM 和 Swap 抖动。 | 🟡 |
| OPS-03 | 初始安全组包含无用 RDP 3389，且项目默认 Compose 会映射后端、数据库和 Redis 端口。 | 云主机默认规则与开发 Compose 不适合生产。 | 删除 RDP；公网只保留 22/80/443；生产覆盖配置不对外暴露 8080、PostgreSQL 和 Redis。 | ✅ |
| OPS-04 | 附件目录没有生产持久化卷，重建容器可能丢附件。 | 初始 Compose 更偏本地开发。 | `docker-compose.prod.yml` 增加附件持久化。对应 `ff756ca`。 | ✅ |
| OPS-05 | 生产环境最初没有资源限制、日志轮转和完整部署覆盖。 | 缺少面向低配单机的生产配置。 | 增加 CPU/内存限制、日志轮转、Nginx 配置和环境模板。对应 `ff756ca`。 | ✅ |
| OPS-06 | 没有域名时，如果直接使用 HTTP 登录，邮箱密码和 Cookie 不安全。 | 生产 Cookie 需要 HTTPS，而域名/备案尚未准备。 | 已加入公网 IP TLS 启用脚本和自动续期单元；但真实证书签发与全链路访问尚未在对话中完成验收。 | 🟡 |
| OPS-07 | 国内服务器拉取 Docker Hub 镜像超时，生产构建被中断。 | 中国大陆访问 Docker Hub 不稳定。 | 仓库增加镜像加速 daemon 配置；对话曾在 ACR 专属加速与 DaoCloud 公共代理之间等待选择。服务器构建仍未形成成功闭环证据。 | 🔴 |
| OPS-08 | 远程部署需要密码反复登录，存在泄露与操作成本。 | 初始只有 root 密码登录。 | 建立 NoteFlow 专用 SSH 密钥，私钥只留本机；部署期间连接验证成功。 | ✅ |
| OPS-09 | SSH 公钥命令因终端自动换行看起来像 5 行，用户不确定如何执行。 | 长命令展示没有预先说明视觉换行。 | 已解释实际是 3 条命令并指导整段复制；以后优先提供脚本文件或编号步骤。 | ⚪ |
| OPS-10 | 阿里云支付二维码加载失败。 | 支付页面/内置浏览器网络问题，与服务器配置无关。 | 改从普通浏览器和待支付订单入口完成购买。 | ⚪ |
| OPS-11 | 生产部署过程已经配置服务器、同步代码、写入环境并开始构建，但容器未最终启动验收。 | Docker Hub 超时中断，后续提交了配置但没有对话证据表明远端重新部署成功。 | 必须继续完成镜像拉取、`compose up`、迁移、健康检查、HTTPS、登录、AI、附件与重启恢复验收。 | 🔴 |

## 9. Git、交付与跨机器运行问题

| ID | 问题与现象 | 根因 | 处理与当前状态 | 状态 |
| --- | --- | --- | --- | --- |
| GIT-01 | 早期多轮认证、导航和设置修改累计后才提交，最终形成一个 20 文件的大提交。 | “每次修改都提交”的协作规则建立较晚。 | 之后设计稿、AI 草稿修复和生产配置均按主题拆分并写详细提交说明。历史提交无法再细分。 | 🟡 |
| GIT-02 | 工作区同时存在 AI 草稿与部署改动，容易和设计或登录修改混入同一提交。 | 多条任务线并行、工作区未始终保持干净。 | 提交前逐文件审计；AI 草稿和部署分别提交为 `2e8e8e7`、`ff756ca`，设计稿也独立提交。 | ✅ |
| GIT-03 | SMTP 授权码、API Key、JWT 密钥可能被误提交。 | 真实邮件与生产部署都需要本地秘密。 | `.env`、`server/.env` 保持忽略；只提交 `.env.example`，提交前检查未跟踪和忽略文件。 | ✅ |
| GIT-04 | “代码已 push”容易被理解为另一台电脑拉取后可零配置恢复全部系统。 | Git 只保存代码与模板，不保存秘密、数据库卷、Redis、附件和本地对话。 | 已明确：可拉取后创建全新实例，但要复制环境模板并设置密钥；恢复现有实例还需数据库和附件备份。 | ⚪ |
| GIT-05 | 当前数据迁移尚未形成仓库内的一键备份/恢复包。 | 数据和密钥按安全原则不进 Git，但用户有跨机器恢复诉求。 | 已有灾备手册和数据库备份思路；仍需实际导出 PostgreSQL、附件清单和恢复校验。 | 🔴 |
| GIT-06 | 设计稿多轮试错产生 V1–V9，但“哪个是当前有效方向”容易混淆。 | 每轮都保留在仓库，概念探索与可实施方案没有及时标记状态。 | V9 是当前高保真候选，并有独立现有功能审计作为映射基线；V8 只保留为已确认的视觉语言参考，V7 已否决，V1–V6 均为探索或演进对照。V9 仍需用户确认，后续仍建议在设计目录增加统一索引。 | 🟡 |

## 10. 相关提交索引

| 提交 | 内容 | 解决的主要问题 |
| --- | --- | --- |
| `b9c9dd1` | 完善邮箱登录与账号设置 | AUTH-01～AUTH-11、IA-01～IA-05、DEV-05 |
| `4081a9e` | 新增工作台首版高保真设计稿 | UI-01～UI-06 的第一轮探索 |
| `df77bf6` | 探索 AI 原生工作台方案 | UI-13、UI-16 |
| `9c1fb01` | Living Canvas 研究与概念 | UI-16，同时暴露 UI-14 的偏离问题 |
| `4ca7960` | 重绘真实 NoteFlow 工作台 | UI-14，建立真实组件映射 |
| `2e8e8e7` | AI 草稿停止生成与状态恢复 | AI-01～AI-04 |
| `ff756ca` | 生产部署与 IP HTTPS 配置 | OPS-02～OPS-07 |
| `cefe8a6` | 重塑工作台整体画风 V5 | UI-15，建立连续表面与编辑出版式视觉方向 |
| `e990866` | 细化柔滑时尚视觉语言 V6 | UI-17，建立图标、连续曲率、材质与动效细节规则 |
| `cdfacd9` | 重构 NoteFlow 流体编辑工作台 V7 | UI-18，按 `BBC + CBB` 偏好矩阵重构整体布局；后续因成图失真被否决 |
| `design: 重绘精密柔滑工作台 V8` | 使用 HTML/CSS 和矢量图标生成确定性高保真稿 | UI-19、DEV-07、GIT-06 |
| `design: 基于现有功能重排工作台 V9` | 先审计当前可达功能，再以 V8 视觉语言重排主工作台 | UI-20、UI-21、GIT-06 |

## 11. 尚未闭环的优先级

### P0：上线前必须完成

1. 完成阿里云远端镜像拉取和四容器启动，不能只停留在配置提交。
2. 执行数据库迁移，验证后端健康检查与 Nginx 反向代理。
3. 完成公网 IP HTTPS 签发、自动续期和 Secure Cookie 验证。
4. 实测邮箱验证码、密码重置、旧账号登录和会话撤销。
5. 验证 PostgreSQL、附件和配置备份可在隔离环境恢复。
6. 观察 2C2G + Swap 下的内存、日志和容器重启情况。

### P1：下一轮产品改造

1. 以 `design/noteflow-current-function-audit.md` 作为功能映射基线，评审 V9 的入口位置和信息层级；保留 V8 已确认的颜色、字号、材质、图标和动效语言。
2. 优先解决 AI 面板打开后正文过窄、标题重复、修改预览难读三个 P0 体验问题。
3. 再统一目录、编辑器、AI 面板、草稿和修改预览的组件语言。
4. 补搜索初始态、移动端顶栏和深色模式硬编码清理。
5. 每个模块单独提交，附截图、测试和回退说明。

### P2：工程与交付改进

1. 增加开发环境自检，启动前检查 Node、Python、Docker、端口和必需环境变量。
2. 增加远端部署 smoke test 和“生成中停止 → 刷新 → 恢复 → 保存”E2E。
3. 为设计目录增加 `README`，标记当前方案、探索稿和已废弃方向。
4. 提供数据库 + 附件的标准备份、校验和恢复脚本。

## 12. 后续协作规则

结合本轮反复暴露的问题，后续建议固定以下规则：

1. 先明确约束再选方案：用户、预算、设备、部署形态和必须保留的功能先确定。
2. UI 设计必须逐项映射现有组件与真实状态，概念稿不能冒充可实施方案。
3. 设计稿通过后才改业务代码；设计稿、基础 Token、单个模块和运维配置分别提交。
4. 每次提交只包含一个主题，提交信息写清“改了什么、为什么、如何验证”。
5. 不提交秘密与运行数据；需要跨机器迁移时，用独立备份流程解决。
6. “构建通过”不等于“可上线”，必须补真实环境、数据恢复和关键业务链路验收。
7. 临时预览、外部生成服务和镜像代理都属于易失依赖，交付时必须说明当前是否仍可用。

---

本文是截至 2026-09-13 的问题基线，共 69 项问题与决策记录。后续遇到新问题时，应继续沿用现有 ID 分类追加，并在状态变化时同时补充提交号和验证证据。
